<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PayRit" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/black</item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:datePickerDialogTheme">@style/MyDatePickerDialogTheme</item>
    </style>

    <style name="Theme.PayRit" parent="Base.Theme.PayRit" />

    <style name="Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">#22D6D1</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="Login" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/white</item>
        <!-- 스테이터스 바 배경색 설정 -->
        <item name="android:statusBarColor">@color/white</item>
        <!-- 스테이터스 바 글자색을 검정으로 설정 (true일 때 검정) -->
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="MyDatePickerDialogTheme" parent="android:Theme.Material.Light.Dialog">
        <item name="android:colorAccent">@color/primaryMint</item>
        <item name="android:textColorSecondary">@color/black</item>
        <item name="android:buttonBarButtonStyle">@style/MyButtonBarButtonStyle</item>
    </style>

    <style name="MyButtonBarButtonStyle" parent="Widget.Material3.Button.TextButton.Dialog">
        <item name="android:background">@color/white</item>
        <item name="android:textColor">@color/primaryMint</item>
    </style>

    <style name="RoundedCornerMaterialCardView" parent="Widget.MaterialComponents.CardView">
        <item name="shapeAppearance">@style/CustomShapeAppearance</item>
    </style>

    <style name="CustomShapeAppearance" parent="ShapeAppearance.MaterialComponents.MediumComponent">
        <item name="cornerSizeTopLeft">50dp</item>
        <item name="cornerSizeTopRight">50dp</item>
    </style>

    <style name="BottomNavigationViewStyle" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="android:textSize">12sp</item>
        <item name="fontFamily">@font/suit_medium</item>
    </style>

</resources>